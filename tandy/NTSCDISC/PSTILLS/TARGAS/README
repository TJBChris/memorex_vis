The .TGA files in this directory were created by taking the "A" .BMP files
from the PSTILLS directory and converting them using Alchemy, which is
included on this disc.  Once in this format, the images can be processed
by PicLab and other packages.

